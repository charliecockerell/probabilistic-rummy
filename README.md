# probabilistic-rummy

A gin rummy engine and a probabilistic agent that maintains a belief state over the opponent's hand, using their discard choices as the signal. The plan is to build the inference layer first, then the policy that selects actions, then an evaluation harness to measure it. This project is in progress.
